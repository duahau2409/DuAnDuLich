$(document).ready(function () {
    $('.image-slider').slick({
        slidesToShow: 1,
        autoplay: true,
        autoplaySpeed: 1500,
        nextArrow: '.wwanext',
        prevArrow: '.wwaprev',
        dots: true
    });
});
$(document).ready(function () {
    $('.content-slider').slick({
        // slidesToShow: 5,
        // slidesToScroll: 1
        // nextArrow: '.wwanext',
        // prevArrow: '.wwaprev',
        // dots: true
    });
});